---
notes: Pulsar cada chip. Es el mismo registro que usa el campo animation del YAML. Stagger anima hijo por hijo.
---

import AnimationLab from "@/components/content/AnimationLab.svelte";

## Las animaciones son un registro

Cada una es un objeto `keyframes + options`. Elige una y míralo correr; en el YAML es una palabra.

<AnimationLab client:load />
