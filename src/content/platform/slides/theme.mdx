---
notes: Pulsar T. Todo se recolora porque cada swatch lee la variable CSS, nada está duplicado.
---

import ThemeSwatches from "@/components/content/ThemeSwatches.svelte";

## Material Design 3 desde un solo color

La paleta sale de `#00E5C7` con el algoritmo HCT. Pulsa <kbd>T</kbd> y todo se recolora.

<ThemeSwatches client:load />
